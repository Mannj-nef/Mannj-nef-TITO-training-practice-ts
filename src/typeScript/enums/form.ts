export enum FORM {
  LOGIN = "login",
  RESGITER = "register",

  EMAIL = "email",
  PASSWORD = "password",
  CONFIRMPASSWORD = "confirm-password",
}

export enum FORM_EVEN {
  INPUT = "input",
  BLUR = "blur",
  FOCUS = "focus",
}
export enum ACTION_FORM {
  ADD = "ADD",
  UPDATE = "UPDATE",
}
